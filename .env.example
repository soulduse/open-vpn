# WireGuard Host - DDNS domain or public IP
# Example: your-domain.duckdns.org
WG_HOST=your-domain.duckdns.org

# Web UI Password (bcrypt hash)
# Generate hash: docker run --rm ghcr.io/wg-easy/wg-easy wgpw 'YOUR_PASSWORD'
# Use $$ instead of $ for escaping in .env file
PASSWORD_HASH=$$2a$$12$$your-bcrypt-hash-here
